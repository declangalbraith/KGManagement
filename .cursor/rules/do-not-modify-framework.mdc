---
description: 区分框架与业务前端；业务目录下 view/store 可正常迭代；改框架须先征得同意
alwaysApply: true
---

# 框架代码与变更审批

## 原则

- **默认不修改**本仓库中属于「框架 / 上游模板 / 第三方」的代码；后端优先通过**独立 app、配置、扩展点**等方式实现需求。
- **前端**：**当前仓库里已有的前端实现**（见下表「框架」）视为模板/框架，**不要改**；**新业务**放在**业务命名空间**下新建与维护的 `view`、`store` 等**不**视为框架代码，可按业务需求**正常新增与修改**这些业务文件。
- **若评估后认为必须改动框架**（含后端 `dvadmin`、前端非业务目录下的已有文件），必须先向用户**说明原因、影响范围、替代方案**，并**征得明确同意**后再改。

## 在本项目中的范围划分（非穷尽）

| 类型 | 路径 / 说明 |
|------|-------------|
| **框架（前端）** | `web/src` 下**除业务命名空间外的全部已有代码**（如 `views/system/**`、`layout/**`、`components/**`、`router/**`、`stores/*.ts`（根目录下各 store）、`api/**` 中与系统/模板相关的部分等） |
| **业务（非框架，前端）** | 在 **`web/src/views/business/**`** 下的页面与模块，以及在 **`web/src/stores/business/**`** 下的 Pinia store；以及你**明确追加**到本规则中的其他业务目录（例如日后增加 `web/src/api/business/**` 时，可把该路径写进规则） |
| **框架（后端）** | `backend/dvadmin/**` |
| **第三方** | `node_modules/**`、`web/node_modules/**` 等；禁止手改依赖源码 |

若某路径是否算业务不确定，**先询问用户**。

## 代理行为

- 需求落在**框架路径**上时：**停止直接修改**，在回复中列出方案并请用户确认。
- 需求落在**业务命名空间**内时：按正常业务开发处理（可改可增），无需按「改框架」审批。
- 用户已明确授权修改框架时：在授权范围内修改，并标注「已获同意」。
