---
description: 列举仓库目录与框架边界；改框架须先征得同意
alwaysApply: true
---

# 框架代码与变更审批

## 原则

- **默认不修改**本仓库中属于「框架 / 上游模板 / 第三方」的代码；后端优先通过**独立 app、配置、扩展点**（如 `backend/plugins`、`backend/conf`、注册到 `INSTALLED_APPS` 的自建 app）实现需求。
- **前端**：`web/src` 下**既有模板实现**（见下文「`web/src`」）视为框架/模板，**不要改**；**新业务**放在**业务命名空间**（`web/src/views/business/**`、`web/src/stores/business/**` 等）下**新增与维护**，**不**视为框架代码。
- **若评估后认为必须改动框架**（含后端 `backend/dvadmin/**`、前端非业务目录下的已有文件），必须先向用户**说明原因、影响范围、替代方案**，并**征得明确同意**后再改。
- **数据库迁移**：**禁止**直接编辑任意 Django app 下 `migrations/` 中的迁移文件（含已存在的 `*.py`）。模型/schema 变更应通过**先改 `models.py` 等源码**，再在本地执行 `python manage.py makemigrations` / `migrate` 由 Django **生成或更新**迁移；Agent **不**应手改迁移文件来「修」问题。
- **初始化数据（fixtures）**：**禁止**修改任意 **`fixtures/`** 目录下的初始化文件（如 `*.json` 等）。若需调整种子数据，应说明由用户在环境中通过 **`loaddata` / 自定义命令 / 数据迁移策略**处理，或由用户明确授权后再动。

---

## 仓库根目录（仅列目录）

以下为本仓库**根路径下**的一级目录（`.` 自身不列出）。**是否框架**指：是否按「上游 django-vue3-admin / dvadmin 模板」对待、默认不直接改。

| 目录 | 作用 | 是否框架 / 约束 |
|------|------|-----------------|
| `.cursor/` | Cursor 规则、Skills、工作流等编辑器侧配置 | **非框架**；团队/个人工作流，可改 |
| `.git/` | Git 版本库元数据 | **非代码**；勿手改 |
| `.github/` | GitHub Actions 等工作流（如 CI、Playwright） | **非框架**；可改，但改 CI 需知悉对流水线的影响 |
| `.pnpm-store/` | pnpm 本地 store 缓存（若存在） | **生成物/本地缓存**；勿当业务代码维护，通常应忽略 |
| `backend/` | Django 后端：项目入口、`dvadmin`、配置、静态资源等 | **见下节「`backend/`」** |
| `docker_env/` | Docker 构建与运行相关：各服务的 Dockerfile、nginx、mysql 配置等 | **部署/运维模板**；按需调整环境与镜像，**非** `dvadmin` 应用代码；改镜像/端口需与 `docker-compose.yml` 一致 |
| `docs/` | 项目文档 | **非框架**；文档可维护 |
| `web/` | Vue3 + Vite 前端工程 | **见下节「`web/`」** |

**根目录常见文件（非目录）**：`README.md` / `README.zh.md`、`LICENSE`、`NOTICE`、`CHANGELOG.md` 为说明与许可；`docker-compose.yml` 为编排；`init.sh`、`crud-gen.sh` 等为初始化/脚手架脚本——均属**项目元数据或工具脚本**，不是 `dvadmin` 运行时框架；修改脚本前确认对团队流程的影响。

---

## `backend/` 子目录

| 子目录 | 作用 | 是否框架 / 约束 |
|--------|------|-----------------|
| `application/` | Django **项目包**：`settings.py`（引用 `conf.env`）、根 `urls.py`、`wsgi.py` / `asgi.py`、`celery.py` 等 | **项目骨架（模板）**，**不在** `dvadmin/**` 内。新增自建 app、改根路由/中间件时常需改此处；**默认不随意改**，除非为扩展或部署必须；大改需说明影响 |
| `conf/` | 环境配置：`env.py`（由 `env.example.py` 等拷贝），数据库、密钥、调试开关等 | **非框架**；本地/环境差异配置，**勿提交密钥**；属预期会改动的目录 |
| `dvadmin/` | 核心应用包：`system`（用户/菜单/权限等）、`test_app`（示例）、`utils`（中间件、Swagger 等）；各 app 内含 `migrations/`、`fixtures/` | **框架（后端）**；**默认不修改** `dvadmin/**` 源码；其中 **`migrations/`、`fixtures/` 见下节，禁止直接手改** |
| `locale/` | Django `gettext` 翻译（如 `zh_Hans`、`en`） | **模板/项目 i18n**；可增删翻译条目，**非**业务逻辑框架边界的主要矛盾 |
| `logs/` | 运行日志输出目录（若存在） | **运行时产物**；非源码 |
| `plugins/` | **插件目录**：`settings` 会将其加入 `sys.path`，用于挂载可插拔扩展 | **扩展点**；新增插件/业务包可放此处（或独立 app），**不**视为 `dvadmin` 核心框架代码 |
| `static/` | 收集或随仓库提供的静态文件：DRF、drf-yasg、captcha、条款等 | **多为第三方或模板静态资源**；**禁止手改** vendor 文件；若必须替换，说明原因 |
| `templates/` | Django 模板（DRF、条款页、Swagger 等） | **模板层**；与上游一致，**默认少改**；确需定制时按「改框架」流程 |
| `util/` | 项目级杂项工具（如示例 `currency.py`） | **项目工具/示例**，非 `dvadmin` 包内；可按项目需要维护 |
| `venv/` | Python 虚拟环境（若存在于仓库内） | **第三方环境**；**禁止**手改包内代码；用 `requirements.txt` 管理依赖 |

**后端小结**：**严格意义上的「后端框架路径」**为 `backend/dvadmin/**`。其余为项目骨架、配置、扩展点或资源；`conf/`、`plugins/`、自建 app 是**推荐**的业务与部署承载位置。

### 迁移与初始化数据（后端，全仓库适用）

| 类别 | 路径模式 | 约束 |
|------|----------|------|
| **迁移文件** | 任意 Django app 下的 `**/migrations/*.py` | **禁止 AI / 禁止手改**迁移文件本身。应通过修改 **Model** 等定义后，由用户在环境中运行 **`makemigrations`** 生成；部署侧用 **`migrate`** 应用。**禁止**为「对齐数据库」而直接编辑已有迁移内容。 |
| **初始化数据** | 任意 **`**/fixtures/**`**（如 `dvadmin/system/fixtures/*.json`） | **禁止修改**。fixtures 用于可重复加载的初始数据；变更应通过用户指定的流程（如导出/新 fixture、管理命令、或由用户执行 `loaddata` 前的文件维护）。 |

自建业务 app 若包含 `migrations/`、`fixtures/`，同样遵守上表：**不直接改迁移与 fixture 文件**，以命令与模型驱动为准。

---

## `web/` 子目录

| 子目录 | 作用 | 是否框架 / 约束 |
|--------|------|-----------------|
| `node_modules/` | npm/pnpm 安装的依赖 | **第三方**；**禁止**手改 |
| `public/` | 构建时拷贝的公共静态资源（favicon 等） | **模板资源**；可按项目替换图标等，避免改依赖包行为 |
| `src/` | 前端源码（路由、布局、页面、状态等） | **见下「`web/src`」** |
| `tests/` | 前端 E2E 等（如 Playwright、`tests/i18n`） | **非框架**；测试可随需求增改，改测试以验证行为 |

**根配置文件**（如 `vite.config.ts`、`package.json`、`playwright.config.ts`、`.eslintrc.js`、`.env.*`）：**工程配置**，非业务页面；按需调整构建/测试/规范，**不**等同于 `dvadmin` 后端框架，但大范围改构建链需评估影响。

### `web/src/` 子目录

| 子目录 | 作用 | 是否框架 / 约束 |
|--------|------|-----------------|
| `api/` | 与后端交互的 API 封装 | **模板层**；**默认不改**；业务 API 建议放 **`web/src/api/business/**`**（若采用该约定，需在业务需求中维护并可在本规则中保持同步） |
| `assets/` | 图片、字体、iconfont 等 | **模板资源**；可增删资源文件，**避免**改框架引用的约定路径除非有意调整 |
| `components/` | 全局通用组件 | **框架（前端）** |
| `directive/` | 自定义指令 | **框架（前端）** |
| `i18n/` | 国际化入口与各语言包、按页面的文案 | **框架（前端）**；新增文案可扩展，**不要**为业务逻辑在框架目录大规模重构 |
| `layout/` | 布局、顶栏、侧栏、锁屏等 | **框架（前端）** |
| `plugin/` | 权限等前端插件 | **框架（前端）** |
| `router/` | 路由与前后端路由生成 | **框架（前端）** |
| `stores/` | Pinia 根目录下各 store（非 `business` 子目录） | **框架（前端）** |
| `theme/` | 全局样式与主题 SCSS | **框架（前端）** |
| `types/` | TS 类型声明 | **框架（前端）** |
| `utils/` | 请求封装、存储、工具函数等 | **框架（前端）** |
| `views/` | 页面视图 | **见下「`web/src/views`」** |

### `web/src/views/` 子目录

| 子目录 | 作用 | 是否框架 / 约束 |
|--------|------|-----------------|
| `business/` | **业务命名空间**：业务页面、模块（若尚未创建，**可新建**） | **非框架**；业务功能**主要**写在此处 |
| `celery/` | Celery 任务与日志相关管理页 | **模板功能页**；视为框架（前端） |
| `interface/` | 接口相关演示/管理 | **模板功能页**；视为框架（前端） |
| `plugins/` | 插件相关入口页面（若仅 `index.ts` 等） | **模板**；视为框架（前端） |
| `system/` | 系统管理（用户、角色、菜单、字典、日志等） | **框架（前端）** |
| `template/` | CRUD 等演示/模板页 | **框架（前端）** |

### `web/src/stores/` 补充

| 路径 | 约束 |
|------|------|
| `web/src/stores/business/**` | **业务 store**；**非框架**，可正常迭代 |
| `web/src/stores/*.ts`（根下其它 store） | **框架（前端）**；默认不改 |

---

## 范围划分速查

| 类型 | 路径 / 说明 |
|------|-------------|
| **框架（前端）** | `web/src` 下除 **`views/business/**`**、**`stores/business/**`**、以及你**明确追加**的业务路径外的**已有**实现 |
| **业务（非框架，前端）** | **`web/src/views/business/**`**、**`web/src/stores/business/**`**；以及你**明确追加**的路径（如 `web/src/api/business/**`） |
| **框架（后端）** | **`backend/dvadmin/**`** |
| **第三方 / 环境** | `**/node_modules/**`、`backend/venv/**` 等；禁止手改依赖源码 |
| **禁止手改（后端）** | **`**/migrations/*.py`**（须通过 `makemigrations` / `migrate` 维护）；`**/fixtures/**`（初始化数据，禁止直接编辑） |

若某路径是否算业务不确定，**先询问用户**。

---

## 代理行为

- 需求落在**框架路径**上时：**停止直接修改**，在回复中列出方案并请用户确认。
- 需求落在**业务命名空间**内时：按正常业务开发处理（可改可增），无需按「改框架」审批。
- 用户已明确授权修改框架时：在授权范围内修改，并标注「已获同意」。
- 涉及 **Django 迁移**时：**不**直接编辑 `migrations/` 下文件；说明应改哪些 **Model**，并提示用户本地执行 **`makemigrations` / `migrate`**。
- 涉及 **fixtures / 初始化数据**时：**不**编辑 `fixtures/` 下文件；说明预期数据变更，由用户选择 **`loaddata`、导出 fixture、或管理命令**等路径；**用户明确授权改 fixture** 时除外，并标注「已获同意」。
